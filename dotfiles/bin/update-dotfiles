#!/usr/bin/env bash

# Shortcut for: dotfiles-cli update
# See 'dotfiles-cli help' for all available commands.

echo "[+] Updating dotfiles..."
cd ~/.dotfiles
git pull
rcup -v -f -d ~/.dotfiles/dotfiles -t linux-desktop
